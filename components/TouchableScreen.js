import React from 'react';
import {View, TouchableOpacity, StyleSheet} from 'react-native';

const TouchableSearchTab = props => {
    return (
        <View>
            
            <TouchableOpacity />
        </View>

    )
}

const styles = StyleSheet.create({
    screen: {
        flex: 1
    }
})

export default TouchableSearchTab;